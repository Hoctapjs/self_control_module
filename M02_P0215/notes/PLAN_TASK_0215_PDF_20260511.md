# KẾ HOẠCH TRIỂN KHAI TASK 0215 (theo file PDF `0215 task.pdf`)

## 0. File cần đọc trước khi bắt tay

Codex bắt buộc nắm các file sau:

- `addons/M02_P0215/__manifest__.py` — depend & data sequence
- `addons/M02_P0215/models/x_psm_hr_discipline_record.py` — toàn bộ state-machine và `action_psm_*`
- `addons/M02_P0215/models/x_psm_hr_discipline_action.py` — `x_psm_improvement_period`, `x_psm_validity_months`
- `addons/M02_P0215/views/x_psm_hr_discipline_record_views.xml` — form & header buttons
- `addons/M02_P0215/views/x_psm_portal_templates.xml` — UI portal nhân viên & quản lý
- `addons/M02_P0215/controllers/portal.py` — endpoint `/my/discipline/...`
- `addons/M02_P0215/wizards/x_psm_hr_discipline_reject_wizard.py` — pattern wizard nhập lý do
- `addons/M02_P0215/views/x_psm_hr_discipline_reject_wizard_views.xml`
- `addons/M02_P0215/data/email_template_discipline_done.xml` — chứa template `Discipline: Decision Notified`
- `addons/M02_P0215/data/mail_template.xml`
- `addons/M02_P0215/data/email_template_rejection.xml`
- `addons/M02_P0215/data/hr_discipline_action_data.xml`
- `addons/M02_P0215/security/security_rules.xml`
- `addons/M02_P0215/security/ir.model.access.csv`
- `addons/M02_P0215/convention/QUY_UOC_DAT_TEN_VA_RULE.md` — convention prefix `x_psm_`, `action_psm_`

> **Ghi chú trạng thái sẵn có:** Theo `DAILY_REPORT_2026_05_08.md`, 11 task trong PDF lần này **chưa task nào** được đánh dấu đã làm. Báo cáo cũ chỉ mô tả refactor survey/feedback/migration trước đó.

---

## A. NHÓM ƯU TIÊN — LUỒNG STORE LEVEL (làm trước)

### A1. Task 2 — Bỏ survey feedback khi RGM xác nhận "Không tái phạm"

**Mục tiêu:** Khi RGM bấm "Không tái phạm", record không tự gán hành động Feedback và không mở survey nữa. Thay vào đó chuyển sang `level_determination` để RGM tự chọn hình thức.

**File dự kiến sửa:**
- `models/x_psm_hr_discipline_record.py`
  - Sửa `action_psm_rgm_no_repeat` (hiện `state → proposal`, gán `feedback`, mở survey)
- `views/x_psm_hr_discipline_record_views.xml`
  - Cập nhật label nút (Không tái phạm → Xác định hình thức) và đảm bảo nút Store/Company ở `level_determination` hiển thị đúng quyền (đã có `groups="M02_P0200.GDH_OPS_STORE_RGM_M"`)
- `controllers/portal.py`
  - `portal_discipline_rgm_action` nhánh `no_repeat` không chuyển đến survey nữa (chỉ redirect về record)

**Checklist:**
- [ ] `action_psm_rgm_no_repeat` chỉ làm: `state = level_determination`, `x_psm_rgm_is_repeat = False`, KHÔNG ép `x_psm_action_id = feedback`, KHÔNG mở survey, KHÔNG gọi `_x_psm_get_or_create_feedback_survey_user_input`.
- [ ] Cập nhật `_x_psm_allowed_state_transitions()`: `under_review → level_determination` đã cho phép → ok. Kiểm tra thêm `level_determination → proposal` (đã có) còn ổn.
- [ ] Portal route `no_repeat` không gọi `action_psm_open_feedback_survey` nữa.

**Kiểm tra sau khi sửa:**
1. Tạo record draft → "Gửi RGM xem xét" → ở `under_review` nhấn "Không tái phạm" → record phải về `level_determination`, không mở survey.
2. RGM chọn "Store Level" → state = `proposal`, hình thức kỷ luật trống chờ chọn → chọn Feedback → "Ban hành Quyết định" → state = `issued`.

---

### A2. Task 1.a — Store Level không tái phạm: `x_psm_start_date` tính từ lúc nhân viên xác nhận portal

**Mục tiêu:** Khi nhân viên bấm xác nhận trên portal, `x_psm_start_date = today`, `x_psm_end_date` tự động tính theo `x_psm_improvement_period` (đã có `_compute_x_psm_end_date`).

**File dự kiến sửa:**
- `models/x_psm_hr_discipline_record.py` — xác nhận lại `action_psm_employee_accept` đã set `x_psm_start_date = fields.Date.today()`

**Checklist:**
- [ ] `action_psm_employee_accept` đã set `x_psm_start_date = fields.Date.today()` (xác nhận lại — hiện đang đúng tại dòng ~1300).
- [ ] Đảm bảo Store Level cũng đi qua state `notified` để nhân viên bấm chấp nhận (không tự skip về `active`). Hiện `action_psm_submit_for_approval` cho store đi `state = notified` → ok.
- [ ] Loại bỏ phần auto chuyển `active` từ `_x_psm_on_feedback_survey_done` khi đã không còn survey cho no-repeat (đã xử lý ở A1).

**Kiểm tra:**
1. Store no-repeat → tới `notified` → portal nhân viên bấm CHẤP NHẬN → state = `active`, `x_psm_start_date = hôm nay`, `x_psm_end_date` = today + period.

---

### A3. Task 1.b — Store Level tái phạm: tự động điền `x_psm_start_date` = ngày hết hiệu lực record trước + 1 ngày

**Mục tiêu:** Khi nhân viên xác nhận record tái phạm, hệ thống tìm record trước (cùng employee, state `active/expired`, có `x_psm_end_date`) → gán `x_psm_start_date = previous.x_psm_end_date + 1 ngày` thay vì `today`.

**File dự kiến sửa:**
- `models/x_psm_hr_discipline_record.py`
  - Tách helper mới `_x_psm_resolve_effective_start_date(self)`: trả về `previous.x_psm_end_date + 1 ngày` nếu có, ngược lại trả `today`.
  - `action_psm_employee_accept` và bước RGM tự xác nhận khi nhân viên không phản hồi (Task 11.b) đều gọi helper này.

**Checklist:**
- [ ] Truy vấn previous: `state in ('active','expired')`, `x_psm_employee_id = self.x_psm_employee_id`, `id != self.id`, sort `x_psm_end_date desc`, limit 1.
- [ ] Nếu `previous.x_psm_end_date >= today` → start_date = `previous.x_psm_end_date + 1`; nếu nhỏ hơn → start_date = today.
- [ ] `_compute_x_psm_end_date` đã tính `end = start + period` → ok.
- [ ] Ghi `x_psm_previous_record_id = previous.id` để hiển thị cảnh báo tái phạm (đã có ô cảnh báo trong view).

**Kiểm tra:**
1. Tạo nhân viên có 1 record `expired` end_date = 2026-06-10. Tạo record mới → nhân viên chấp nhận hôm nay → start_date = 2026-06-11.
2. Tạo nhân viên có 1 record `expired` end_date = 2025-01-01 → start_date = today.

---

### A4. Task 1.c — Email "Discipline: Decision Notified" thêm 2 trường ngày hiệu lực + ngày kết thúc

**Mục tiêu:** Trong template `email_template_psm_discipline_notified` (file `email_template_discipline_done.xml`), thêm 2 dòng vào `<ul>`: Ngày bắt đầu hiệu lực, Ngày kết thúc hiệu lực. Áp dụng cho mọi luồng (cả store và company).

**File dự kiến sửa:**
- `data/email_template_discipline_done.xml`

**Checklist:**
- [ ] Thêm `<li><strong>Ngày bắt đầu hiệu lực:</strong> <t t-out="object.x_psm_start_date"/></li>`.
- [ ] Thêm `<li><strong>Ngày kết thúc kỷ luật:</strong> <t t-out="object.x_psm_end_date"/></li>`.
- [ ] **LƯU Ý:** Email đang được gửi ở `_x_psm_notify_employee()` trong `action_psm_submit_for_approval` → store level lúc đó chưa có start_date vì nhân viên chưa accept. Phải đẩy việc gửi email "đã ban hành cần xác nhận" và email "đã có hiệu lực" thành 2 template riêng, **hoặc** đổi trigger gửi sang sau khi nhân viên accept (state `active`) cho email có start/end.
- [ ] Khuyến nghị: tách thành `email_template_psm_discipline_active_notified` mới (gửi khi `state = active`) chứa 2 trường ngày, giữ template hiện tại cho thông báo "đã ban hành cần xác nhận".

**Kiểm tra:**
1. Hoàn tất luồng Store no-repeat → nhân viên chấp nhận → kiểm tra email gửi tự động chứa đầy đủ Hình thức, Ngày ban hành, Ngày bắt đầu hiệu lực, Ngày kết thúc.
2. Kiểm tra cho cả luồng tái phạm Store và Company.

---

### A5. Task 3 — Cho phép RGM ký tên trên internal + thêm trường Người làm chứng

**Mục tiêu:** Trong form RGM (internal view), trường `x_psm_manager_signature` và `x_psm_employee_signature` hiện đang `readonly="1"` → cho phép sửa. Thêm trường `x_psm_witness_id`, `x_psm_witness_signature` luôn hiển thị (không phụ thuộc `invisible="not x_psm_witness_id"`).

**File dự kiến sửa:**
- `views/x_psm_hr_discipline_record_views.xml` — page `signatures`

**Checklist:**
- [ ] Bỏ `readonly="1"` trên 2 widget signature manager + employee — có thể giới hạn theo state (chỉ edit khi `state in ('draft','under_review')`).
- [ ] Group "Người làm chứng" bỏ `invisible="not x_psm_witness_id"`, thêm field `x_psm_witness_id` ở chế độ edit.
- [ ] Hiển thị thêm thông tin phụ trợ: `x_psm_witness_id.department_id`, `x_psm_witness_id.job_id`, level (giống cách hiển thị Nhân viên).

**Kiểm tra:**
1. Mở record draft trên internal (RGM) → ký vào canvas signature → save → ảnh chữ ký lưu thành công vào field.
2. Chọn witness employee → group hiển thị thông tin bộ phận/vị trí; ký witness → save thành công.

---

### A6. Task 4 — Bỏ 2 group `FEEDBACK SURVEY` và `MIGRATION` trên form view

**File dự kiến sửa:**
- `views/x_psm_hr_discipline_record_views.xml`

**Checklist:**
- [ ] Xoá block `<group string="Feedback Survey" ...>` và `<group string="Migration" ...>`.
- [ ] Cân nhắc xoá nút stat `Feedback` và nút `Bridge dữ liệu cũ` ở header nếu không còn ý nghĩa với end-user (giữ lại nếu cần debug với `groups="hr.group_hr_manager"`).
- [ ] KHÔNG xoá field model (`x_psm_feedback_*`, `x_psm_migration_*`) vì còn tham chiếu trong logic / dữ liệu cũ — chỉ ẩn khỏi view.

**Kiểm tra:**
1. Mở 1 record bất kỳ → form không còn hiện 2 group này.
2. Module vẫn upgrade được không lỗi.

---

### A7. Task 6 — Bắt buộc ký Bản tường trình khi xác nhận ngoài giao diện portal

**Mục tiêu:** Trên portal, form gửi Section II (`/my/discipline/submit_section_ii`) phải kiểm tra có `employee_signature` (base64) trước khi submit; backend reject nếu thiếu.

**File dự kiến sửa:**
- `views/x_psm_portal_templates.xml` — block `employee_sig_form` thêm validate JS + ràng buộc required.
- `controllers/portal.py` — `portal_discipline_submit_section_ii` thêm check signature length, từ chối nếu thiếu (redirect kèm error message).

**Checklist:**
- [ ] JS validate (đã có pattern tương tự cho form create — copy logic) trước submit form `employee_sig_form`.
- [ ] Backend: nếu `not post.get("employee_signature") or len(post["employee_signature"]) < 500` → redirect kèm `?error=missing_signature`.
- [ ] Template hiển thị thông báo lỗi nếu có query string `error`.
- [ ] Validate tương tự cho `manager_finalize`.

**Kiểm tra:**
1. Đăng nhập portal bằng nhân viên → nhập feedback nhưng không ký → bấm "GỬI" → form chặn (JS alert).
2. Tắt JS → POST raw không có chữ ký → backend redirect lỗi, record không bị write.

---

## B. NHÓM CHUNG (ảnh hưởng cả 2 luồng)

### B1. Task 8 — Bắt nhập lý do khi từ chối

Có 2 sub-case:

**B1a. HR bấm "Không cần họp"** (state `investigation` → `proposal`)
- Thêm wizard nhập lý do hoặc mở rộng `x_psm_hr_discipline_reject_wizard`.
- Thêm field model mới: `x_psm_skip_hearing_reason = fields.Text(...)` lưu lý do.
- View: nút "Không cần họp" type `object` mở wizard thay vì gọi trực tiếp.

**B1b. CEO bấm "CEO Từ chối"** (Approval flow)
- Hiện `action_psm_ceo_reject` không ép nhập lý do.
- Sửa để mở wizard nhập `x_psm_approval_refuse_reason` trước khi đổi state.

**File dự kiến sửa/thêm:**
- `wizards/x_psm_hr_discipline_reject_wizard.py` — mở rộng wizard với `mode` (`explanation` / `skip_hearing` / `ceo_reject`).
- `views/x_psm_hr_discipline_reject_wizard_views.xml`
- `models/x_psm_hr_discipline_record.py`
- `views/x_psm_hr_discipline_record_views.xml`

**Checklist:**
- [ ] Wizard có field `x_psm_reason` required; tạo các method `action_skip_hearing()` và `action_ceo_reject()`.
- [ ] Nút view ở state `investigation` "Không cần họp" → trả `act_window` mở wizard mode `skip_hearing`.
- [ ] Nút "CEO Từ chối" → mở wizard mode `ceo_reject`.
- [ ] Khi xác nhận wizard: ghi reason vào field tương ứng, sau đó gọi method state-machine cũ.

**Kiểm tra:** Bấm 2 nút từ chối, đảm bảo bắt nhập reason, reason lưu được vào chatter.

---

## C. NHÓM LUỒNG COMPANY LEVEL (làm sau)

### C1. Task 5 — Rà soát phân quyền nút trong stage Investigation (Xác minh nâng cao)

**File dự kiến sửa:**
- `views/x_psm_hr_discipline_record_views.xml`
- Có thể bổ sung `security/security_rules.xml`

**Checklist:**
- [ ] Hiện 2 nút `Cần họp kỷ luật` và `Không cần họp` đều gắn `groups="M02_P0200.GDH_RST_HR_HRBP_S"`. Xác nhận nhóm này có chứa đủ HR/OC không.
- [ ] Theo PDF Bước 4: HR/OC vào cuộc → nên cho phép cả HR group và OC group.
- [ ] Đảm bảo nút Hủy trong state này có quyền phù hợp.

**Kiểm tra:** Đăng nhập 3 role (HR, OC, RGM, Crew) → mở record state investigation → kiểm tra button visibility.

---

### C2. Task 7 — Khi HR bấm "Cần họp kỷ luật": kiểm tra meeting đã tạo chưa

**Mục tiêu:** Khi state = `investigation` và HR bấm "Cần họp kỷ luật" (`action_psm_schedule_hearing`), nếu chưa có `calendar.event` liên kết → hiển thị cảnh báo / mở form tạo meeting, gán attendees, send email.

**File dự kiến sửa:**
- `models/x_psm_hr_discipline_record.py` — `action_psm_schedule_hearing`
- (tuỳ chọn) wizard nhỏ `x_psm.hr.discipline.meeting.wizard` để chọn attendees & date

**Checklist:**
- [ ] `action_psm_schedule_hearing` trước khi đổi state → đếm `calendar.event` link tới record (`res_model`, `res_id`). Nếu 0 → trả về `act_window` mở form `calendar.event` với `default_res_model`, `default_res_id`, `default_partner_ids` từ employee + HR + OC + công đoàn.
- [ ] Sau khi user save meeting → trigger `template.send_mail` cho attendees; hoặc dựa vào `mail_message` auto của calendar.
- [ ] Khi đã có meeting → cho phép bấm để đổi state `hearing`.
- [ ] Thêm `UserError` thay vì silent skip.

**Kiểm tra:**
1. State `investigation` không có meeting → bấm "Cần họp" → mở form calendar.event.
2. Tạo meeting xong, quay lại record → bấm lần nữa → state đổi sang `hearing`.

---

### C3. Task 9 — Thêm nút "Xem xét lại" ở State 6 (Proposal) cho luồng Company

**Mục tiêu:** Khi record đang ở `proposal` và `x_psm_discipline_level = 'company'` (đã đi qua họp), RGM bấm "Xem xét lại" → record về `level_determination` để RGM xác định lại Store/Company.

**File dự kiến sửa:**
- `models/x_psm_hr_discipline_record.py`
- `views/x_psm_hr_discipline_record_views.xml`

**Checklist:**
- [ ] Thêm method `action_psm_rgm_recheck_level(self)` → `_x_psm_write_state('level_determination', {'x_psm_action_id': False})`.
- [ ] Cập nhật `_x_psm_allowed_state_transitions`: `proposal` → cho phép `level_determination`.
- [ ] Thêm button view: `invisible="state != 'proposal' or x_psm_discipline_level != 'company'"`, group RGM.
- [ ] Khi RGM "Ban hành Quyết định" thì `x_psm_action_id` bắt buộc — đã ép bằng `_x_psm_ensure_action_selected`.

**Kiểm tra:** Tạo record Company đi đến state `proposal` → bấm "Xem xét lại" → state `level_determination`, `x_psm_action_id` rỗng. Đi lại đường Store/Company.

---

### C4. Task 10 — CEO Từ chối → record về state 7 (`issued`) để RGM chọn lại Hình thức

**Mục tiêu:** Hiện tại CEO reject đưa về `proposal`. Đổi: về `issued` cùng việc clear `x_psm_action_id` để RGM chọn lại; ghi `x_psm_approval_refuse_reason`.

**Lưu ý:** Transitions hiện tại: `approval → proposal` đã có; `approval → issued` chưa có. Cần thêm vào `_x_psm_allowed_state_transitions`. Cần cho phép quay vòng `issued → approval` (đã có).

**File sửa:**
- `models/x_psm_hr_discipline_record.py`: `action_psm_ceo_reject`, `_x_psm_on_approval_request_refused`, `_x_psm_allowed_state_transitions`
- Kết hợp với B1b (yêu cầu nhập lý do CEO Từ chối)

**Checklist:**
- [ ] Allowed transitions thêm: `approval → issued`.
- [ ] `_x_psm_on_approval_request_refused` đổi target từ `proposal` → `issued`, clear `x_psm_action_id`, ghi reason từ wizard.
- [ ] View nút ở `issued`: thêm hint hiển thị `x_psm_approval_refuse_reason` nếu có.

**Kiểm tra:** Luồng Company → CEO Từ chối + nhập lý do → state `issued`, `x_psm_action_id` rỗng, có thể chọn lại và gửi duyệt vòng 2.

---

### C5. Task 11 — Nhân viên từ chối (Company): bắt nhập lý do + về state 4, mở quyền RGM xác nhận hộ

**Mục tiêu:**
- Khi nhân viên từ chối trên portal: bắt buộc nhập `x_psm_employee_response_note`. Nếu `x_psm_discipline_level = 'company'` → state về `investigation` (Bước 4) thay vì `proposal`. Thông báo cho Line manager + RGM + HR (mail + activity).
- "Kết quả xử lý" trên portal chỉ hiển thị khi nhân viên xác nhận.
- Nếu nhân viên không phản hồi → RGM có thể bấm nút "Xác nhận hộ" và nhập `x_psm_start_date`.

**File sửa:**
- `controllers/portal.py` — `portal_discipline_respond`
- `views/x_psm_portal_templates.xml` — block respond nhân viên (required cho textarea khi reject), điều kiện hiển thị "Kết quả xử lý" thêm `t-if="record.x_psm_is_accepted"`
- `models/x_psm_hr_discipline_record.py` — `action_psm_employee_reject`: thêm logic rẽ nhánh theo `x_psm_discipline_level`; thêm `action_psm_rgm_manual_confirm()` cho RGM
- `views/x_psm_hr_discipline_record_views.xml` — thêm nút "RGM xác nhận hộ" với điều kiện `state = 'notified' AND not x_psm_is_accepted` AND group RGM

**Checklist:**
- [ ] Portal `reject` không có note → trả lỗi (400 hoặc redirect lỗi).
- [ ] Allowed transitions: `notified → investigation` (cho company), `notified → proposal` (cho store — đã có).
- [ ] Khi reject ở luồng Company: gửi `mail.activity` cho Line manager (parent_id), RGM (group GDH_OPS_STORE_RGM_M), HR (group HR Manager).
- [ ] Nút "RGM xác nhận hộ" mở wizard nhỏ nhập `x_psm_start_date` (mặc định = today), sau đó set state `active`.
- [ ] Portal template "Kết quả xử lý" thêm `t-if="record.x_psm_is_accepted"`.

**Kiểm tra:**
1. Company → nhân viên từ chối không note → form portal chặn.
2. Company → nhân viên từ chối có note → state về `investigation`, activity tới HR/RGM.
3. Store → nhân viên từ chối có note → state về `proposal` (giữ logic cũ).
4. State `notified` + nhân viên chưa accept → RGM thấy nút "Xác nhận hộ" → bấm, nhập ngày → state `active`.

---

## D. Quy trình kiểm thử tổng (sau khi xong từng nhóm)

### 1. Restart + upgrade module:
```powershell
d:\odoo-19.0+e.20250918\odoo-bin -c d:\odoo-19.0+e.20250918\odoo.conf -u M02_P0215 --stop-after-init
```

### 2. Smoke test parse:
- `python -m py_compile` cho từng `.py` đã sửa
- Mở Odoo log đảm bảo không có XML parse error

### 3. Ba kịch bản đầu-cuối (chạy bằng tay sau từng nhóm):

**Kịch bản 1 — Store không tái phạm:**
Manager tạo log (portal) → RGM "Không tái phạm" → state `level_determination` → RGM chọn Store → Feedback → Ban hành → nhân viên portal Chấp nhận → state `active`, start/end đúng, email có 2 trường ngày.

**Kịch bản 2 — Store tái phạm:**
Tạo nhân viên có expired trước → record mới → RGM "Có tái phạm" → Store → chọn Counseling 1st → Ban hành → nhân viên Chấp nhận → start_date = previous.end_date + 1.

**Kịch bản 3 — Company đầy đủ:**
RGM "Có tái phạm" → Company → Investigation → HR "Cần họp" (check meeting wizard) → tạo meeting → close hearing → Proposal → RGM "Xem xét lại" → quay về level_determination → đi lại Company → CEO Từ chối (wizard reason) → state `issued` → RGM chọn lại → CEO Phê duyệt → nhân viên Từ chối (reason) → state `investigation` → ... → cuối cùng nhân viên Chấp nhận → `active` với start/end đúng.

### 4. Kiểm tra phân quyền:
Đăng nhập 4 role (Crew, Manager/MIC, RGM, HR Manager) → test visibility các nút.

### 5. Kiểm tra portal:
- Validate signature `employee_sig_form` và `manager_sig_form`
- Reject flow yêu cầu note
- "Kết quả xử lý" chỉ hiện khi accepted

---

## E. Thứ tự đề xuất commit cho Codex

| # | Nhóm | Task | Loại |
|---|------|------|------|
| 1 | A6 | Task 4 — Xóa group Feedback Survey + Migration trên view | UI nhẹ |
| 2 | A1 | Task 2 — Bỏ survey, đi `level_determination` | Logic + UI |
| 3 | A2 + A3 | Task 1.a + 1.b — start_date | Logic |
| 4 | A4 | Task 1.c — Email Notified thêm 2 trường ngày | Data XML |
| 5 | A5 | Task 3 — Ký internal + witness | UI |
| 6 | A7 | Task 6 — Ràng buộc ký portal | Controller + JS |
| 7 | B1 | Task 8 — Wizard reason cho skip_hearing + ceo_reject | Wizard |
| 8 | C1 | Task 5 — Phân quyền nút investigation | Security |
| 9 | C2 | Task 7 — Check meeting trước hearing | Logic + Wizard |
| 10 | C3 | Task 9 — Nút "Xem xét lại" | Logic + UI |
| 11 | C4 | Task 10 — CEO reject về `issued` | Logic |
| 12 | C5 | Task 11 — Nhân viên reject Company về `investigation` + RGM xác nhận hộ | Logic + Controller + UI |

---

## F. Ghi chú task đã làm trước đây (bỏ qua)

Sau khi đối chiếu `DAILY_REPORT_2026_05_08.md` và các file `PHASE_*.md` trong thư mục `notes/`: **không task nào trong PDF lần này đã được implement**. Toàn bộ 11 task đều cần triển khai mới.

> **Cảnh báo quan trọng:** Trường `x_psm_action_id` đang dùng domain filter `[('x_psm_level', '=', x_psm_discipline_level)]` ở proposal — nên Task 9 (clear `x_psm_action_id` khi về level_determination) sẽ hoạt động đúng. Nhưng kiểm tra lại transitions trong `_x_psm_allowed_state_transitions` mỗi khi thêm chiều mới — đây là điểm dễ bị `UserError` ngoài ý muốn.
